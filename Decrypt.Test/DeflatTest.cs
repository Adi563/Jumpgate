using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Decrypt.Test
{
    [TestClass]
    public class DeflatTest
    {
        [TestMethod]
        public void TestMethod1()
        {
            byte[] data = new byte[] { 0x03, 0x20, 0x04, 0x06, 0x04, 0xeb, 0x2d, 0x52, 0xdf, 0xa3, 0x5f, 0x87, 0x30, 0x71, 0x09, 0x49, 0x20, 0x26, 0x0d, 0x38, 0x3c, 0xba, 0x98, 0xf3, 0x01, 0x00, 0x01, 0x00, 0x4f, 0x81, 0xc9, 0x05, 0xbf, 0xfb, 0x38, 0xf6, 0xbe, 0x5b, 0x42, 0x78, 0x59, 0xbb, 0x2b, 0x6c, 0x4e, 0x60, 0xce, 0x8b, 0xde, 0xcd, 0x62, 0xab, 0x44, 0xb6, 0xa9, 0x85, 0x4b, 0xf6, 0x62, 0xd2};
            var streamCompressed = new System.IO.MemoryStream(data);
            //var streamDecompressed = new System.IO.MemoryStream();
            

            var deflateStream = new System.IO.Compression.DeflateStream(streamCompressed, System.IO.Compression.CompressionMode.Decompress);
            //deflateStream.CopyTo(streamDecompressed);

            byte[] buffer = new byte[100];
            int bytesRead = deflateStream.Read(buffer, 0, buffer.Length);
        }
    }
}